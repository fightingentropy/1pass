# Generate a long random value and keep the real value only in .dev.vars and
# the Cloudflare Pages secret store. It is required once for setup/migration.
BOOTSTRAP_SECRET=replace-with-a-long-random-secret
